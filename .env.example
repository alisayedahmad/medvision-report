# Copy to .env and fill in. Never commit the real .env file.

# LLM provider for report generation (Phase 4)
ANTHROPIC_API_KEY=
# or OPENAI_API_KEY=

# API service (Phase 6)
API_KEY_SECRET=

# Kubernetes / monitoring (Phase 7)
PROMETHEUS_PUSHGATEWAY_URL=
